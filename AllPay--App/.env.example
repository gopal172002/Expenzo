# Copy to .env and adjust as needed:
#   copy .env.example .env

# Production (Render)
API_BASE_URL=https://expenzo-gchd.onrender.com/api
USE_LOCAL_API=false

# Local development — dashboard backend on port 5000
# API_BASE_URL=http://localhost:5000/api
# USE_LOCAL_API=true

# Optional sync secret (match backend MOBILE_SYNC_SECRET)
MOBILE_SYNC_SECRET=
